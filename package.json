{
  "name": "app",
  "version": "1.0.0",
  "description": "Cloudflare Workers environment in Rust",
  "main": "build/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
